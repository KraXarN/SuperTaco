template = {}

function template.load()
	-- Load code, set vars etc.
end

function template.move(key)
	-- (Optional) If a key gets pressed, for holding, use update
end

function template.draw()
	-- What to draw
end


function template_update(dt)
	-- Combine all events for update
end

function template_keypress(key)
	-- Combine all events for keypresses
end

function template_draw()
	-- Combine all events for draw
end